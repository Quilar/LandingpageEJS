<head>
    <link rel="stylesheet" href="styles/testimonials.css">
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="scripts/testimonials-carousel.js"></script>
</head>
<section class="section-container testimonial-container">
  <img src="/content/images/elbphilharmonie.jpg" alt="" class="section-bkg testimonial-bkg"/>
  <section class="testimonials-section">
    <h2>Das sagen unsere Nutzer</h2>
    <div class="testimonials">
      <span class="handle testimonials-handle-left">&#8249;</span>
        <div class="testimonials-slider">
          <div class="testimonial-items">
            <% if (testimonials.length > 0) { %>
              <% testimonials.forEach( doc => { %>
                <div class="testimonial-item" id="<%=doc.id%>">
                  <img src="content/testimonials/<%=doc.image%>"/>
                  <p>"<%= doc.testimonie %>"</p>
                  <span><%= doc.name %></span>
                </div>
              <% }) %>
            <% } else{ %>
              <p>Keine Daten verfügbar</p>
            <% } %>
          </div>          
        </div>
        <span class="handle testimonials-handle-right">&#8250;</span>
        </div>
    <div class="testimonials-dots">
      <% if (testimonials.length > 0) { %>
        <% testimonials.forEach( doc => { %>
          <img class="testimonial-dot<%= (doc.id === 0) ? ' active-dot' : '' %>"/>
        <% }) %>
      <% } %>
    </div>

</section>
</section>